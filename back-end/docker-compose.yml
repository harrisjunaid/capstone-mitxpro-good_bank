version: '3.4'

services:
  capstone_server_1:
    image: goodbank_server:5051
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 5051:5051
